import * as React from 'react';
import ReactDOM from 'react-dom';

const App: React.FC = () => {
    return (
        <button>hello all over the world</button>
    )
}

ReactDOM.render(<App />, document.getElementById('root'));